# 「まず5分」Netlify設定
# サイト構成:
#   index.html                      ← mazu5min.html を改名して配置
#   netlify.toml                    ← このファイル
#   netlify/functions/decompose.js  ← AI分解の中継Function

[build]
  publish = "."

[functions]
  directory = "netlify/functions"
